<p id="notice"><%= notice %></p>

<p>
  <strong>Uid:</strong>
  <%= @identity.uid %>
</p>

<p>
  <strong>Service:</strong>
  <%= link_to @identity.service.name, @identity.service %></td>

</p>

<p>
  <strong>User:</strong>
  <%= link_to @identity.user.username, @identity.user %></td>

</p>

<%= link_to 'Edit', edit_identity_path(@identity) %> |
<%= link_to 'Back', identities_path %>
